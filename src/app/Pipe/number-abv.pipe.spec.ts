import { NumberAbvPipe } from './number-abv.pipe';

describe('NumberAbvPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberAbvPipe();
    expect(pipe).toBeTruthy();
  });
});
